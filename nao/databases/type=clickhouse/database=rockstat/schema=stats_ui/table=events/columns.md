---
type: generated
comment: This file is managed and generated by the agent, do not modify it!
---

# events

**Dataset:** `stats_ui`

## Description

_No description available._

## Table Metadata

| Property | Value |
|----------|-------|
| **Row Count** | 813,714,102 |

## Columns (54)

- projectId (UInt32 NOT NULL)
- date (Date NOT NULL)
- dateTime (DateTime NOT NULL)
- ts (UInt64 NOT NULL)
- uid (UInt64 NOT NULL)
- sess_start (UInt64 NOT NULL)
- name (LowCardinality(String) NOT NULL)
- is_page (UInt8 NOT NULL)
- host (LowCardinality(String) NOT NULL)
- path (String NOT NULL)
- query (String NOT NULL)
- title (String NOT NULL)
- url (String NOT NULL)
- ref (String NOT NULL)
- ref_host (LowCardinality(String) NOT NULL)
- sess_num (UInt16 NOT NULL)
- sess_pageNum (UInt16 NOT NULL)
- sess_eventNum (UInt16 NOT NULL)
- sess_type (LowCardinality(String) NOT NULL)
- sess_engine (LowCardinality(String) NOT NULL)
- sess_refhost (LowCardinality(String) NOT NULL)
- utm_source (LowCardinality(String) NOT NULL)
- utm_medium (LowCardinality(String) NOT NULL)
- utm_campaign (LowCardinality(String) NOT NULL)
- utm_content (LowCardinality(String) NOT NULL)
- utm_term (LowCardinality(String) NOT NULL)
- pid (LowCardinality(String) NOT NULL)
- cid (String NOT NULL)
- user_id (String NOT NULL)
- is_auth (Int8 NOT NULL)
- locale (LowCardinality(String) NOT NULL)
- currency (LowCardinality(String) NOT NULL)
- language (LowCardinality(String) NOT NULL)
- tz (LowCardinality(String) NOT NULL)
- build_version (LowCardinality(String) NOT NULL)
- country (LowCardinality(String) NOT NULL)
- region (LowCardinality(String) NOT NULL)
- city (LowCardinality(String) NOT NULL)
- browser (LowCardinality(String) NOT NULL)
- browser_version (LowCardinality(String) NOT NULL)
- os (LowCardinality(String) NOT NULL)
- os_version (LowCardinality(String) NOT NULL)
- device_type (LowCardinality(String) NOT NULL)
- device_vendor (LowCardinality(String) NOT NULL)
- device_model (LowCardinality(String) NOT NULL)
- screen_w (Int16 NOT NULL)
- screen_h (Int16 NOT NULL)
- is_bot (Int8 NOT NULL)
- is_webview (Int8 NOT NULL)
- threat_score (Float32 NOT NULL)
- asn_org (LowCardinality(String) NOT NULL)
- ip (String NOT NULL)
- lib_v (UInt32 NOT NULL)
- props (Map(String, String) NOT NULL)



## Indexes and table definition

Use this when writing queries: filter using the most selective ORDER BY, or PARTITION BY columns, PROJECTIONS, or PRIMARY KEY to minimize scanned data and improve performance.

```
CREATE TABLE stats_ui.events
ENGINE = MergeTree
PARTITION BY toYYYYMM(date)
ORDER BY (projectId, date, dateTime, uid)
TTL date + toIntervalDay(90)
SETTINGS index_granularity = 8192
```

